// UI string catalog — currently used minimally since each language has its own App component
// that embeds its own UI strings. Reserved for shared cross-cutting strings (alerts, errors).
export const UI_STRINGS = {
  // Reserved for future use. Add bilingual strings here as { key: { en, zh } }
  // Example:
  //   exportSuccess: { en: 'Export complete', zh: '导出成功' },
};
